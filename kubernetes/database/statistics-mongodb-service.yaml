apiVersion: v1
kind: Service
metadata:
  name: statistics-mongodb
  namespace: piggymetrics
spec:
  type: ClusterIP
  selector:
    app: statistics-mongodb
  ports:
  - name: mongodb
    port: 27017
    targetPort: 27017